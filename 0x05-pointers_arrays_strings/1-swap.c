#include "PandAHeaders.h"

/**
 * swap_int - is the name of the function
 * @a: is the parameter of the function
 * @b: is the parameter of the function
 * Describition: function to swap values
 */

void swap_int(int *a, int *b)
{
	*a = *b;
	*b = *a;
}
