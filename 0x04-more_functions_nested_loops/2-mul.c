#include "main.h"

/**
 * mul - is the name of the function
 *
 * @a: is the parametar of the function
 * @b: is the parameter of the function
 * Description: the function is to multiply two digits
 * Return: is used to return the result of multiplicaion
 */

int mul(int a, int b)
{
	return (int (a * b));
}
