#include <stdio.h>
/**
 * main - Entry point
 * @argc: is the name of the parameter
 * @argv: is the name of the parameter
 * Return: 0
 */
int main(int argc, char **argv)
{
	(void) argv;
	printf("%d\n", argc - 1);
	return (0);
}
